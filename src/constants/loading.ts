/** Minimum time auth skeleton stays visible so it can be perceived. */
export const AUTH_INIT_MIN_SKELETON_MS = 450;

/** Minimum time route content skeleton stays visible during lazy loads. */
export const ROUTE_SKELETON_MIN_MS = 350;
