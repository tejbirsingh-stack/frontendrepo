const axios = require('axios');
(async () => {
  try {
    // 1. authenticate (we don't have token easily, maybe just check the server route directly)
    console.log('Skipping API test, need token');
  } catch (e) {
    console.error(e);
  }
})();
